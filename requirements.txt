# Denaro — dipendenze runtime (allineate a pyproject.toml)
ccxt>=4.0
python-dotenv>=1.0
PyYAML>=6.0
pydantic>=2.0
psutil>=5.9
pyzmq>=25.0
websockets>=12.0

# Sviluppo / test
pytest>=8.0
pytest-asyncio>=0.23
