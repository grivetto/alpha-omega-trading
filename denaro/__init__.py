"""Denaro — package principale.

Contiene:
- motori live flat (engine_solo_v33, engine_paper, infra_*) — invariati, in produzione
- `domain/`   — layer dominio (types, indicators, risk, grid) — Fase 3, puro Python
- `application/` — orchestrazione (Fase 3, in costruzione)
- `infrastructure/` — adattatori (Fase 3, in costruzione)
- `tests/`    — test di parita' e invarianti (Fase 3)
"""
