# ============================================================
# env.live.example — struttura operativa LIVE (MAI committare i valori reali)
# Copiare le righe nel .env del nodo corrispondente, DOPO aver creato le chiavi.
# ============================================================

# --- MARCODG1 (87.106.222.123) — Bitvavo ---
# Crea la chiave su Bitvavo -> API -> New Key
# Permessi: SOLO View + Trading (MAI Withdraw)
# IP Whitelist: 87.106.222.123
BITVAVO_API=
BITVAVO_SECRET=

# --- NUVOLA (87.106.3.15) — Kraken ---
# Crea la chiave su Kraken -> Settings -> API
# Permessi: Query Funds + Query Open/Closed Orders + Create/Cancel Orders
#            (MAI Withdraw, MAI Deposit)
# IP Whitelist: 87.106.3.15
KRAKEN_API=
KRAKEN_SECRET=

# ============================================================
# DOPO aver scritto le chiavi, verifica READ-ONLY (non piazza ordini):
#   /usr/bin/python3 -c "
#   import ccxt,os
#   for l in open('.env'):
#       l=l.strip()
#       if l and not l.startswith('#') and '=' in l:
#           k,v=l.split('=',1); os.environ[k]=v
#   ex=ccxt.bitvavo({'apiKey':os.environ['BITVAVO_API'],'secret':os.environ['BITVAVO_SECRET'],'enableRateLimit':True})
#   print(ex.fetch_balance()['free'])"
# ============================================================
