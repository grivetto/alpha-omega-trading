# 2026-05-23

## Fixed: PnL Computation in memorize_trades
- Rewrote FIFO matching to use cycle-based approach:
  - **Grid pattern** (stellatron): buy→sell, profit = sell_val - buy_val - fees
  - **Reversal pattern** (marco_sol, orion): sell→buy, profit = prior_sell_val - buy_val - fees
- Uses real fee values per trade (not hardcoded 0.225%)
- Same-side sequences (two buys or two sells in a row) get 0 PnL

## Fixed: Fee Floor in strategy_optimizer
- Added `MIN_SPACING = 0.003` (0.3%) as hard floor for stellatron grid_spacing
- This prevents optimizer from producing sub-fee spacing (was 0.0012 = 0.12% < 0.15% fee round-trip)
- Both profit_pct and grid_spacing now stay above fee floor

## Already Applied (previous session, now committed):
- stellatron: daily loss limit bug fix (compare against total_invested, not session PnL)
- stellatron: max_invested_eur 50→25€ to free EUR liquidity

## Push
- `76187782` pushed to both `dollari` and `alpha-omega-trading`
- Files: memorize_trades.py, stellatron.py, strategy_optimizer.py

## Note
- Local run of optimizer works (fee floor active) but DB has 0 trades (not on this machine)
- Still need to deploy memorize_trades + optimizer fixes to mc2
- Console encoding issue (cp1252 vs → arrow) — skip on windows, works on linux/mc2
