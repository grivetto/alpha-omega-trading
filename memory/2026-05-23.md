# 2026-05-23

## Deployed to mc2 — Latest Fixes

### Files Updated via git checkout from dollari/main:
- `memorize_trades.py` — cycle-based PnL matching (reset net_pnl=NULL first)
- `strategy_optimizer.py` — fee floor MIN_SPACING=0.003
- `denaro_memory.py` — NULL-safe get_trade_stats, COALESCE in get_recent_trades
- `orchestrator.py` — latest version with 8 API endpoints

### Bugs Fixed (3 commits pushed today):
1. `981679d8` — PRAGMA table_info col name: `row[1]` not `d[0]` (integer)
2. `7d8441e5` — `COALESCE(net_pnl,0) AS net_pnl` alias in get_recent_trades
3. `c519acb7` — NULL filter in get_trade_stats pnls list
4. `275d2e86` — Reset net_pnl=NULL before cycle matching in memorize_trades

### PnL Recalculation (302 trades):
- **Before**: -653€ (wrong FIFO matching)
- **After**: +127.41€ (cycle-based, still approximate for historical partial fills)
- marco_sol: sells +35.63, buys -27.37 = net +8.26
- orion: sells +133.53, buys -14.38 = net +119.15

### Params Generated + Synced:
- `params_stellatron.json` → nuvola (grid_spacing=0.003)
- `params_marco_sol.json` → MARCODG1 (sell_raise=0.0023, buy_drop=-0.002)
- `params_orion.json` → local on mc2 (defaults, neutral regime)

### Cron Jobs Active (all 4):
- memorize_trades: every 1 min
- regime_detector: every 5 min
- strategy_optimizer: every 30 min
- capital_manager: every 30 min

### Orchestrator (denaro-dashboard.service) restarted
- All 8 API endpoints verified working
- Dashboard loads, PnL visible

### Nuovo: Analisi alpha-omega-trading ricevuta
- Riguarda codice vecchio (denaro_v2), non i bot attuali
- Punti validi: trailing SL, ATR spacing, OCO orders, short in mean rev
- Da valutare per futura iterazione dei bot attuali
